<%= form_for(@join_ticker_player) do |f| %>
  <% if @join_ticker_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@join_ticker_player.errors.count, "error") %> prohibited this join_ticker_player from being saved:</h2>

      <ul>
      <% @join_ticker_player.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ticker_id %><br />
    <%= f.number_field :ticker_id %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.number_field :player_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
