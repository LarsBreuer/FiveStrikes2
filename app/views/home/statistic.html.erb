
<% if @game %>

  <table div id="game_item_table">
    <% @tickers.each do |ticker| %>
      <tr  class="<%= cycle('firstrow', 'secondrow') -%>">
        <td><%= ticker.convert_seconds_to_time(ticker.time) %></td>    
        <td><%= @game.count_team_goals_time(@game.team_home_id, ticker.time) %>:<%= @game.count_team_goals_time(@game.team_away_id, ticker.time) %></td>
        <td><%= ticker.player.player_forename %> <%= ticker.player.player_surename %></td>
      </tr>
    <% end %>
  </table>

  <table div id="game_item_table">
	<tr align="right">
		<td><%= link_to 'Home', home_statistic_home_path(:game_id=>@game.id), :remote => true %></td>
	</tr>
  </table>

<% end %>