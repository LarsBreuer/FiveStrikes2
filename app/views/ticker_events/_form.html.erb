<%= form_for(@ticker_event) do |f| %>
  <% if @ticker_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticker_event.errors.count, "error") %> prohibited this ticker_event from being saved:</h2>

      <ul>
      <% @ticker_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br />
    <%= f.number_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.number_field :time %>
  </div>
  <div class="field">
    <%= f.label :ticker_event_note %><br />
    <%= f.text_area :ticker_event_note %>
  </div>
  <div class="field">
    <%= f.label :activity_id %><br />
    <%= f.number_field :activity_id %>
  </div>
  <div class="field">
    <%= f.label :home_or_away %><br />
    <%= f.number_field :home_or_away %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
