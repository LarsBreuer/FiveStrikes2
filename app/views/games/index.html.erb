<h1>Listing games</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Home</th>
    <th>-</th>
    <th>Away</th>
    <th>Halbzeitlänge</th>
    <th>User</th>
    <th>Datum</th>
  </tr>

<% @games.each do |game| %>
  <tr>
    <td><%= game.id %></td>
    <td><%= game.club_home_name %></td>
    <td></td>
    <td><%= game.club_away_name %></td>
    <td><%= game.duration_halftime %></td>
    <td><%= game.user.name %></td>
    <td><%= game.game_date %></td>
    <td><%= link_to 'Show', game %></td>
    <td><%= link_to 'Edit', edit_game_path(game) %></td>
    <td><%= link_to 'Destroy', game, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>

</table>

<br />

<%= link_to 'New Game', new_game_path %>
